annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.0
certifi==2026.7.22
click==8.5.0
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
pydantic==2.13.5
pydantic_core==2.46.5
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.4
pypdf==6.18.0
pymupdf==1.28.2
python-multipart==0.0.32
python-dotenv==1.2.3

# 레이아웃 검출용 (선택). 미설치 시 PyMuPDF 기하 판단으로 동작합니다.
# Windows 네이티브에서는 설치가 까다로우니 WSL 또는 Docker를 권장합니다.
#   python -m pip install paddlepaddle-gpu==3.3.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu130/
#   python -m pip install "paddleocr[all]"
numpy>=1.26
pillow>=10.0

# --- 시스템 의존성 (pip으로 설치되지 않음) ---
# .docx/.doc/.pptx 업로드를 PDF로 변환하는 데 LibreOffice(soffice 실행 파일)가
# 필요합니다. pip 패키지가 아니라 OS 패키지 매니저로 설치해야 합니다.
#   Debian/Ubuntu: apt-get install -y libreoffice
#   Docker 이미지라면 Dockerfile에 위 설치 단계를 추가하세요.
# 설치되어 있지 않으면 해당 확장자 업로드 시 500 오류로 안내됩니다 (.pdf/.txt/
# .md/.png/.jpg/.jpeg 등 다른 형식에는 영향이 없습니다).